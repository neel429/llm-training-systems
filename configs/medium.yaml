# Medium experiment: GPT-2-medium (345M), 4 GPUs, FSDP BF16, 1024 seq_len
# Run: NUM_GPUS=4 STRATEGY=fsdp DTYPE=bf16 ./launch.sh --config configs/medium.yaml

model:      gpt2-medium
strategy:   fsdp
dtype:      bf16
batch_size: 4
seq_len:    1024
steps:      100
grad_accum: 4
lr:         2.0e-4
